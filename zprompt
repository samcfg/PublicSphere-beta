We should make sure that moderators only recieve appended attribution tables, where the anonymous ones 1
user_id optional parameter in language? ramafications? 
For entities that have user_id=null, this displays as "deleted" in UI. I think "anonymous" is better. 
● Component libraries:
  - shadcn/ui (headless, customizable,
  Tailwind-based—very popular)
  - Radix UI (primitives, accessibility-focused)
  - Mantine (full-featured, good defaults)
  - Ant Design (enterprise-grade, comprehensive)
 
  curl -X POST http://localhost:8000/api/users/register/ \
    -H "Content-Type: application/json" \
    -d '{"username": "testuser2", "email": "test2@example.com", "password": "testpass123", "password_confirm": "testpass123"}'
 

- Play with border colors
testing: 
  - We need to link anon of nodes and their connections "from" more closely, specifically during creation
  - Need to make rating more interactive
  - 
- Iron out comments
  - anon purple
  - borders (meaning, expand the DOM width to give frame buttons L and R?)
- Test actual functionality, contexts, working
- Sidebar (more like a transparent sidebar in which nodes hover in)
- Alternate node and connection view!!!!!!!